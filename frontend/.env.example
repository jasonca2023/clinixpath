# Optional. Enables city autocomplete on the location field; without it the
# field is a plain text input and everything else works unchanged.
# Free key: https://myprojects.geoapify.com/
VITE_GEOAPIFY_API_KEY=your_key_here
